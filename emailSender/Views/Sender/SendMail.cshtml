
@model emailSender.Models.EmailFormModel

<br />
<div class="container">
    @*<table class="table table-bordered">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Apellido Paterno</th>
                    <th>Apellido Materno</th>
                    <th>email</th>
                    <th>Action</th>

                </tr>
            </thead>
            <tbody>

                @for (int i = 0; i < Model.Count(); i++)
                {
                    <tr>
                        <td>@Model[i].nombre</td>
                        <td>@Model[i].a_paterno</td>
                        <td>@Model[i].a_materno</td>
                        <td>@Model[i].email</td>
                        <td>
                            <a href="@Url.Action("Delete", new { id = Model[i].id})"><i class="glyphicon glyphicon-trash"></i></a>
                            <a href="@Url.Action("Edit", new { id = Model[i].id})"><i class="glyphicon glyphicon-edit"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>*@
    <style>
        .layout{
            display: flex;
            color: black;
            padding: 5px 5px 5px 5px;
        }
    </style>

    <div class="form-group layout">
        <label>Mensaje:</label>
        <textarea id="w3mission" class="form-control" name="Mensaje" placeholder="Mensaje" rows="4" cols="50">
        </textarea>
        @if (ViewBag.UserList != null)
        {
            @Html.DropDownListFor(model => model.id, ViewBag.UserList as SelectList, "Selecciona un destino", new { @class = "form-control" })
        }
        <div class="button">
            <button>Submit</button>
        </div>
    </div>
  
 


</div>

